const name = require('./name');
const adjective = require('./adjective');
const verb = require('./verb');
const complement = require('./complement');

module.exports = {
    name, adjective, verb, complement,
};
